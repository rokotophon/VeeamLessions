using System;

namespace CalcLib
{
    public static class Calc
    {    
        public static double MyExp(double x) => Math.Exp(x);
        public static double MyLog(double x) => Math.Log(x);
        public static double MyAbs(double x) => Math.Abs(x);

    }
}
